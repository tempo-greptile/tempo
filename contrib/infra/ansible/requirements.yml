#SPDX-License-Identifier: MIT-0
---
# Ansible Galaxy requirements for Tempo infrastructure

roles:
  # Tailscale VPN setup
  - name: artis3n.tailscale
    version: "5.1.0"

collections:
  # Grafana collection for monitoring setup
  - name: grafana.grafana
    version: ">=2.0.0"