import TokenWatchApproveParams from '../../../../snippets/sdk/params/token.watchApprove.mdx'
import TokenWatchApproveReturns from '../../../../snippets/sdk/returns/token.watchApprove.mdx'

# `token.watchApprove`

Watches for token approval events on TIP20 tokens.

## Usage

:::code-group

```ts twoslash [example.ts]
import { client } from './viem.config'

const unwatch = client.token.watchApprove({
  token: 0n,
  onApproval: (args, log) => {
    console.log('Amount:', args.amount)
    console.log('Owner:', args.owner)
    console.log('Spender:', args.spender)
  },
})

// Later, stop watching
unwatch()
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
// [!include ~/snippets/viem.config.ts:setup]
```

:::

## Return Type

<TokenWatchApproveReturns />

## Parameters

<TokenWatchApproveParams />
