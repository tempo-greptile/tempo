### `cancel`

```ts
type ReturnType = 0x${string}
```

Returns a transaction hash.

### `cancelSync`

```ts
type ReturnType = {
  orderId: bigint
  /** Transaction receipt. */
  receipt: TransactionReceipt
}
```
